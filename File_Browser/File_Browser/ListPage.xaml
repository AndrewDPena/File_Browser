<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:File_Browser"
             xmlns:helpers="clr-namespace:File_Browser.ViewHelpers;assembly=File_Browser"
             x:Class="File_Browser.ListPage">
<ContentPage.Resources>
    <helpers:DateTimeConverter x:Key="DateTimeConverter"></helpers:DateTimeConverter>
</ContentPage.Resources>

    <StackLayout>
        <ListView x:Name="ListView" ItemsSource="{Binding FileList}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding FileName}" 
                              Detail="{Binding LastEdited, Converter={StaticResource DateTimeConverter}, StringFormat='Last Modified: {0}'}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>
